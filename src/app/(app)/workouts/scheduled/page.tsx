import PageBody from "@/components/layout-related/page-body";
import ScheduledWorkouts from "@/components/app/workouts/scheduled/scheduled-workouts";

const Page = () => {
  return (
    <PageBody>
      <ScheduledWorkouts />
    </PageBody>
  );
};

export default Page;
