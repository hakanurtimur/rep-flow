import PageBody from "@/components/layout-related/page-body";
import WorkoutSession from "@/components/app/workout-session/workout-session";

const Page = () => {
  return (
    <PageBody>
      <WorkoutSession />
    </PageBody>
  );
};

export default Page;
